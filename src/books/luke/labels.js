// A readable name for every icon this reader shows, keyed by Commons file title. They name
// the place and the date, not the Gospel, so a file shared with another reader usually keeps
// the same name here.
module.exports={
};
