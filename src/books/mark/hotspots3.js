// Positioned markers: [label, explanation, top%, left%]. Coordinates are read off a 10% grid
// over src/img/<key>.webp and checked by drawing them back (BOOK=mark python3 src/tools/overlay.py).
// Keep every coordinate inside 9–92; assemble.js clamps outside that.
module.exports={
};
